import { ScrollArea } from "@mantine/core";

const MainConversation = () => {
	return (
        <ScrollArea h={"calc(100vh - 18.85rem)"}>
            <div>hi</div>
        </ScrollArea>
    );
};

export default MainConversation;
